# Configs

Planned config groups:

- dataset configs
- split configs
- normalization configs
- model configs
- training configs

These should be saved with every run so training can be resumed or compared
later without depending on notebook memory.

