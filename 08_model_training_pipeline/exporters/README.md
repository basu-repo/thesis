# Exporters

This package will convert raw simulation data into standardized episode frames.

Planned stages:

1. read bag data
2. align ego state, goal, obstacle observations, and optional agent context
3. write one episode-frame export per run
4. make the output independent of notebook state

