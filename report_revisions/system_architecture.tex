% A4-friendly layered system architecture figure
% Assumes the main document already loads:
% \usepackage{tikz}
% \usetikzlibrary{arrows.meta}

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    >=Latex,
    every node/.style={font=\small},
    layer/.style={
        rectangle,
        rounded corners,
        draw=blue!65!black,
        thick,
        fill=cyan!16,
        align=center,
        text width=12cm,
        minimum height=0.9cm
    },
    box/.style={
        rectangle,
        rounded corners,
        draw=black!55,
        thick,
        fill=white,
        align=center,
        text width=3.1cm,
        minimum height=0.95cm
    },
    widebox/.style={
        rectangle,
        rounded corners,
        draw=black!55,
        thick,
        fill=white,
        align=center,
        text width=3.7cm,
        minimum height=0.95cm
    },
    arrow/.style={->, thick, draw=blue!70!black}
]

% Layer 1
\node[layer] (simlayer) at (0,0) {\textbf{Simulation Layer}};
\node[box] (world) at (-4.1,-1.15) {Custom world\\\texttt{sim\_world.sdf}};
\node[box] (models) at (0,-1.15) {Vehicle models\\Husky UGV + M100 UAV};
\node[box] (sensors) at (4.1,-1.15) {Sensor streams\\odom, scan, point cloud, state};

% Layer 2
\node[layer] (rtlayer) at (0,-3.0) {\textbf{Middleware and Runtime Layer}};
\node[box] (bridge) at (-4.1,-4.15) {\texttt{ros\_gz\_bridge}\\topic bridging};
\node[box] (ros2) at (0,-4.15) {ROS~2 runtime\\control, sync, logging};
\node[box] (bag) at (4.1,-4.15) {Bag recording\\episode metadata};

% Layer 3
\node[layer] (datalayer) at (0,-6.0) {\textbf{Data Processing and Learning Layer}};
\node[widebox] (seq) at (-4.1,-7.15) {Sequential dataset\\CNN-LSTM export};
\node[box] (graph) at (0,-7.15) {Graph dataset\\\texttt{frames.jsonl}};
\node[widebox] (train) at (4.1,-7.15) {Offline training\\and evaluation};

% Layer 4
\node[layer] (deploylayer) at (0,-9.0) {\textbf{Deployment Layer}};
\node[widebox] (offline) at (-2.6,-10.15) {Offline comparison\\CV / CNN-LSTM / GNN-LSTM / CNN-GNN-LSTM};
\node[widebox] (live) at (2.6,-10.15) {Live deployment\\trained checkpoints in simulation};

% Main layer arrows
\draw[arrow] (simlayer.south) -- (rtlayer.north);
\draw[arrow] (rtlayer.south) -- (datalayer.north);
\draw[arrow] (datalayer.south) -- (deploylayer.north);

% Internal arrows
\draw[arrow] (world.south) -- (bridge.north);
\draw[arrow] (models.south) -- (ros2.north);
\draw[arrow] (sensors.south) -- (bag.north);

\draw[arrow] (bridge.south) -- (seq.north);
\draw[arrow] (ros2.south) -- (graph.north);
\draw[arrow] (bag.south) -- (train.north);

\draw[arrow] (seq.south east) -- (offline.north west);
\draw[arrow] (graph.south) -- (offline.north);
\draw[arrow] (train.south west) -- (live.north east);

\end{tikzpicture}
\caption{Layered system architecture of the experimental platform. Ignition
Gazebo provides the world, robot models, and sensor streams; ROS~2 manages
runtime communication and logging; offline scripts construct learning datasets;
and trained checkpoints support both comparative evaluation and live
deployment.}
\label{fig:system_architecture}
\end{figure}
